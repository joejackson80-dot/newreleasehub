{
  "name": "newreleasehub-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "cd apps/web && npx prisma generate && npx next build"
  },
  "devDependencies": {
    "next": "16.2.4"
  }
}
