import VerifiedDiscoveryClient from './VerifiedDiscoveryClient';

export const metadata = {
  title: 'Verified Discovery | NRH Network Authority',
  description: 'Advanced artist discovery and network equity analysis terminal.'
};

export default function VerifiedDiscoveryPage() {
  return <VerifiedDiscoveryClient />;
}
