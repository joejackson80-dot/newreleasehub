import React from 'react';
import MerchStorefrontClient from './MerchStorefrontClient';

export const metadata = {
  title: 'Network Logistics | NRH',
  description: 'Institutional asset drops and reputation-gated merchandise.',
};

export default function NetworkMerchPage() {
  return <MerchStorefrontClient />;
}
