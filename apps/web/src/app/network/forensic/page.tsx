import ForensicDiscoveryClient from './ForensicDiscoveryClient';

export const metadata = {
  title: 'Forensic Discovery | NRH Network Authority',
  description: 'Advanced artist discovery and network equity analysis terminal.'
};

export default function ForensicDiscoveryPage() {
  return <ForensicDiscoveryClient />;
}
